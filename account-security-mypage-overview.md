# 계정·보안·마이페이지 한눈에 보기

## 1. 무엇을 제공하나요?

MatchEAT의 계정 영역은 다음 기능을 담당합니다.

- 회원가입과 로그인
- 내 정보 및 비밀번호 변경
- 판매자 신청과 관리자 승인
- 구매·판매·제안·채팅 내역 조회
- 회원 탈퇴와 활성 거래 확인
- 상품·거래·채팅 신고
- 관리자 신고 처리와 계정 정지
- 정지 사유 안내와 사용자 해명 메시지

## 2. 사용한 기술

| 구분 | 기술 | 사용 목적 |
|---|---|---|
| 언어 | Java 17, JavaScript | 서버 기능과 화면 동작 구현 |
| 서버 | Spring Boot 4.1, Spring MVC | 웹 페이지와 REST API 제공 |
| 인증 | Spring Security, JWT, BCrypt | 로그인, 권한 검사, 비밀번호 보호 |
| 데이터 | Spring Data JPA, PostgreSQL | 회원·신고·제재 데이터 저장 |
| 마이그레이션 | Flyway | 계정과 신고 관련 DB 구조 관리 |
| 화면 | Thymeleaf, HTML, CSS, JavaScript | 로그인·마이페이지·관리자 화면 구성 |
| 실시간 기능 | Spring WebSocket | 기존 채팅 기능 연동 |
| API 문서 | Springdoc OpenAPI | Swagger 기반 API 확인 |
| 테스트 | JUnit 5, Mockito, Spring Security Test, H2, Node Test | 서비스·보안·화면 회귀 검증 |

기존 독립 로그인 모듈은 참고용으로만 남아 있으며 메인 `settings.gradle` 빌드에는 포함되지 않습니다. 실제 기능은 메인 프로젝트의 `domain/account` 영역에서 동작합니다.

## 3. 주요 API

### 인증과 내 계정

| API | 설명 |
|---|---|
| `GET /api/v1/auth/email-availability` | 이메일 사용 가능 여부 확인 |
| `POST /api/v1/auth/signup` | 회원가입 |
| `POST /api/v1/auth/login` | 로그인 및 JWT 발급 |
| `GET /api/v1/account/me` | 내 프로필 조회 |
| `PATCH /api/v1/account/me` | 이름 변경 |
| `PATCH /api/v1/account/me/password` | 비밀번호 변경 및 기존 JWT 무효화 |
| `DELETE /api/v1/account/me` | 활성 거래 확인 후 회원 탈퇴 |
| `POST /api/v1/account/seller-applications` | 판매자 등록 신청 |

### 마이페이지 목록

| API | 설명 |
|---|---|
| `GET /api/v1/orders/purchases` | 구매 거래 조회 |
| `GET /api/v1/orders/sales` | 판매 거래 조회 |
| `GET /api/v1/proposals/received` | 받은 제안 조회 |
| `GET /api/v1/proposals/sent` | 보낸 제안 조회 |
| `GET /api/v1/chat-rooms` | 참여 채팅방 조회 |

목록 API는 필요할 때 `page`, `size`, `status` 조건을 받을 수 있습니다. 기존 화면과의 호환을 위해 조건이 없는 호출은 기존 배열 형식을 유지합니다.

### 신고와 정지

| API | 설명 |
|---|---|
| `POST /api/v1/reports` | 신고 본문·스냅샷·이미지 접수 |
| `GET /api/v1/reports/mine` | 신고 처리 결과만 조회 |
| `GET /api/v1/admin/reports` | 관리자 신고함 조회 |
| `PATCH /api/v1/admin/reports/{reportId}` | 신고 상태와 처리 결과 변경 |
| `POST /api/v1/admin/reports/{reportId}/penalties` | 기간 정지 부과 |
| `PATCH /api/v1/admin/users/{userId}/status` | 수동 정지 또는 해제 |
| `POST /api/v1/auth/suspension/appeals` | 정지 사용자의 해명 메시지 제출 |

관리자 API는 `ADMIN` 역할만 사용할 수 있습니다. 일반 계정 API는 로그인한 활성 사용자만 접근할 수 있습니다.

## 4. 사용자 작업 흐름

### 가입과 로그인

1. 사용자가 이메일, 이름, 비밀번호를 입력합니다.
2. 서버가 형식, 중복, 비밀번호 정책을 확인합니다.
3. 비밀번호는 BCrypt 해시로 저장합니다.
4. 로그인에 성공하면 JWT를 발급하고 이후 요청에서 사용자 ID와 역할을 확인합니다.

### 마이페이지 확인

1. JWT의 사용자 ID로 내 프로필을 불러옵니다.
2. 주문·제안·거래·채팅 API에서 내 데이터만 조회합니다.
3. 원본 거래 상태를 제안, 진행, 완료, 종료처럼 간단하게 표시합니다.
4. 화면으로 돌아오거나 일정 시간이 지나면 최신 상태를 다시 확인합니다.
5. 일부 API가 실패해도 정상 조회된 목록은 그대로 보여줍니다.

### 비밀번호 변경과 탈퇴

1. 비밀번호 변경 시 현재 비밀번호를 다시 확인합니다.
2. 변경 후 기존 JWT를 사용할 수 없게 만들고 다시 로그인하도록 안내합니다.
3. 탈퇴 시에도 현재 비밀번호를 확인합니다.
4. 진행 중인 거래가 있으면 탈퇴를 막아 거래 데이터가 중간에 끊기지 않게 합니다.

## 5. 신고·관리자 작업 흐름

### 신고 접수

1. 사용자가 상품, 거래 또는 채팅에서 신고 버튼을 누릅니다.
2. 서버가 사용자가 해당 대상과 실제 관계가 있는지 확인합니다.
3. 신고 내용, 당시 데이터 스냅샷, 선택 이미지를 함께 저장합니다.
4. 같은 대상을 반복 신고하거나 한 시간에 10건을 넘기면 차단합니다.
5. 신고 내용과 증거는 관리자만 확인하며 사용자에게는 처리 결과만 보여줍니다.

### 관리자 처리와 제재

1. 관리자는 새 신고 뱃지와 신고함에서 대기 건을 확인합니다.
2. 신고를 검토 중, 완료 또는 반려 상태로 변경하고 답변을 기록합니다.
3. 필요한 경우 피신고자에게 1·3·7·15·30일 정지를 부과합니다.
4. 기간 정지는 만료 시 자동 해제됩니다.
5. 수동 정지는 사유를 반드시 기록하고 관리자가 직접 해제할 때까지 유지됩니다.

### 정지 사용자 안내

1. 정지 사용자가 올바른 비밀번호로 로그인해도 일반 JWT는 발급하지 않습니다.
2. 정지 안내 화면으로 이동해 사유와 종료 시각을 보여줍니다.
3. 사용자는 비밀번호를 다시 확인한 뒤 관리자에게 해명 메시지를 보낼 수 있습니다.

## 6. 보안상 중요한 점

- 사용자 ID와 신고 대상자는 화면 입력을 신뢰하지 않고 JWT와 서버 데이터로 결정합니다.
- 정지·탈퇴 계정은 JWT 보호 API를 사용할 수 없습니다.
- 비밀번호 변경, 탈퇴, 정지 해명처럼 중요한 작업은 비밀번호를 다시 확인합니다.
- 관리자 처리와 신고 상태 변경은 이력으로 남깁니다.
- 신고 이미지의 형식, 크기, 개수를 서버에서 검사합니다.
- 수동 정지와 기간 정지가 겹쳐도 기간 만료가 수동 정지를 해제하지 않습니다.

## 7. 현재 범위와 후속 개선

현재 구현은 기능과 보안 요구를 충족하며 기존 API와 화면 호환성을 유지합니다. 다만 마이페이지는 출처별 첫 100건을 받아 화면에서 나누므로 장기 운영 전 DB 커서 페이징, 서버 상태 필터 연결, 증분 상태 갱신이 필요합니다.

상세 구현은 `final-account-mypage-implementation-spec.md`, 성능 개선 계획은 `mypage-long-term-optimization-report.md`에서 확인할 수 있습니다.
